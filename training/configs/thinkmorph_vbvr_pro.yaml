unified_edit:
  dataset_names:
    - vbvr_pro
  image_transform_args:
    image_stride: 16
    max_image_size: 1024
    min_image_size: 512
  vit_image_transform_args:
    image_stride: 14
    max_image_size: 518
    min_image_size: 224
  is_mandatory: true
  # Repeating shard paths here guarantees enough work units for the documented
  # multi-node setups. The iterable dataset itself runs until total_steps.
  num_used_data:
    - 256
  weight: 1
